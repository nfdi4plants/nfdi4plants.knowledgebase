---
title: ARCitect Installation
lastUpdated: 2025-07-03
authors:
  - dominik-brilhaus
sidebar:
    order: 2
---

import { Steps } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

import GitDownloadInstall from '../git/git-download.mdx'
import GitCheck from '../git/git-check.mdx'

ARCitect depends on <a href="https://git-scm.com/downloads" target="_blank">Git</a> and <a href="https://git-lfs.com" target="_blank">Git LFS</a>.  
The ARCitect installer for Windows includes both tools automatically.  
On macOS and Linux, Git and Git LFS have to be installed separately.

<details>
    <summary>Check git installation</summary>
    <GitCheck />
</details>


<Tabs syncKey="os">
<TabItem label="Windows" icon='seti:windows'>

<Steps>
1. Head to the ARCitect's GitHub [release page](https://github.com/nfdi4plants/ARCitect/releases/latest).
2. Under "Assets" Download the latest release for Windows (`ARCitect-x.x.x-win.zip`).
3. In Windows Explorer, open the folder where you downloaded the ARCitect.
4. Unzip (`Right click` ➞ `Extract All..`) the folder (`ARCitect-x.x.x-win.zip`). You can move this folder to another location (e.g. `Program files`).
5. Start the ARCitect, by clicking the `ARCitect` application.

6. Once started, you see a Windows Defender warning. To proceed, click `More info` and then `Run anyway`.

    ![](@images/arcitect/win-defender.png)

7. Windows Defender Firewall further asks you to allow network communication. This enables communication with the [DataHUB](/nfdi4plants.knowledgebase/datahub).

    ![](@images/arcitect/win-firewall.png)

    :::tip
    You might have to select the app from your taskbar.
    :::
</Steps>

</TabItem>
<TabItem label="Macos" icon="apple">

### Git Installation

<GitDownloadInstall />

### ARCitect Installation

<Steps>

import MacOSPopup from "@images/arcitect/macos-popup-dev.png"
import MacOSSecurity from "@images/arcitect/macos-security.png"
import MacOSPopupNetwork from "@images/arcitect/macos-popup-network.png"

1. Head to the ARCitect's GitHub <a href="https://github.com/nfdi4plants/ARCitect/releases/latest" target="_blank">release page</a>.

2. Download the macOS installer `ARCitect-x.x.x.dmg`. 

3. Double-click the installer `ARCitect-x.x.x.dmg` and move the app to your Applications folder.

</Steps>

### Unidentified developer

When starting ARCitect for the first time, you are warned about an unidentified developer.

<img src={MacOSPopup.src} style="width:25%;display: block; margin: 20px" />

Please navigate to the Privacy & Security panel in your System Settings to `Open Anyway`.

<img src={MacOSSecurity.src} style="width:50%;display: block; margin: 20px" />


### Accept incoming network connections

Every time you start the ARCitect, you are asked to accept incoming network connections.

<img src={MacOSPopupNetwork.src} style="width:25%;display: block; margin: 20px" />

This enables communication with the [DataHUB](/nfdi4plants.knowledgebase/datahub).

</TabItem>

<TabItem label="Linux" icon='linux'>

### Git Installation

<GitDownloadInstall />

### ARCitect Installation

:::note[Installation now based on DEB package]
To avoid issues on certain Linux distributions, ARCitect installation is based on DEB package (instead of AppImage) since version 0.0.59. 
:::

<Steps>

1. Install additional dependencies via
    
    ```bash
    sudo apt-get update
    sudo apt-get install libgtk-3-0 libnotify4 xdg-utils libsecret-1-0
    ```

    :::note
    On some linux distributions you have to run `sudo apt --fix-broken install` afterwards.
    :::

2. Head to the ARCitect's GitHub <a href="https://github.com/nfdi4plants/ARCitect/releases/latest" target="_blank">release page</a>.
3. Download the Linux DEB package `ARCitect-x.x.x_amd64.deb`.
4. Install ARCitect

    ```bash
    sudo dpkg -i ./ARCitect-x.x.x_amd64.deb
    ```

5. Start the ARCitect via

    ```bash
    arcitect
    ```

</Steps>
    
</TabItem>
</Tabs>